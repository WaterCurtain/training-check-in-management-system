# 电气仪控实训室训练管理系统 PRD

> 文档名称：PRD.md  
> 产品名称：电气仪控实训室训练管理系统  
> 产品形态：本地部署 Web 系统（手机端 + 管理端）  
> 目标用户规模：约 15 人  
> 部署方式：实训室 Windows 电脑本地部署，局域网访问  
> 数据存储：SQLite + 本地照片目录  
> 文档版本：V1.0  
> 更新日期：2026-09-19

---

## 1. 项目背景

实训室目前需要对约 15 名成员的日常训练情况进行规范化记录，包括：

- 每次开始实训时间；
- 每次结束实训时间；
- 单次实训时长；
- 每日累计实训时长；
- 每周累计实训时长；
- 每月累计实训时长；
- 月度训练时长是否达标；
- 实训频率；
- 现场照片留档；
- 历史记录查询；
- 月度统计与本地导出。

现有人工签到、纸质登记或普通 Excel 记录方式存在以下问题：

1. 无法准确记录开始和结束时间；
2. 人工计算训练时长工作量大，容易出错；
3. 无法快速查看某人的日、周、月训练情况；
4. 难以直观看出月度训练是否达标；
5. 现场照片与签到记录难以对应；
6. 月底统计、汇总、导出效率低；
7. 异常记录如忘记结束打卡，不易规范处理和追溯。

因此计划开发一套面向实训室内部使用的轻量级 Web 系统。

---

# 2. 产品目标

## 2.1 核心目标

建设一套低成本、可长期运行、部署简单的实训室训练管理系统，实现：

1. 手机端快速完成开始/结束实训打卡；
2. 自动计算单次、每日、每周、每月实训时长；
3. 自动判断月度实训总时长是否达标；
4. 每次打卡支持上传现场照片；
5. 实时查看个人训练数据；
6. 管理员统一查看所有成员训练情况；
7. 支持表格、图表形式的数据可视化；
8. 支持 Excel / CSV 等格式导出；
9. 支持照片和数据库本地存储；
10. 无强制云服务、无长期订阅费用。

---

# 3. 产品范围

## 3.1 V1.0 范围

V1.0 包含以下功能：

### 用户端

- 姓名识别/选择；
- 所属车间信息；
- 开始实训打卡；
- 结束实训打卡；
- 开始打卡拍照；
- 结束打卡拍照；
- 今日实训时长；
- 本周实训时长；
- 本月实训时长；
- 月度目标完成率；
- 月度达标状态；
- 实训次数；
- 历史实训记录；
- 单次实训详情。

### 管理端

- 人员管理；
- 实训记录管理；
- 数据总览；
- 日/周/月统计；
- 月度达标统计；
- 实训频率统计；
- 异常记录管理；
- 管理员补录；
- 记录修改；
- 审计日志；
- Excel 导出；
- CSV 导出；
- 照片查看；
- 数据备份。

---

## 3.2 V1.0 暂不包含

以下功能不纳入首版：

- 微信小程序；
- 原生 Android / iOS App；
- 人脸识别；
- GPS 定位；
- NFC 打卡；
- 指纹识别；
- 短信验证码；
- 云端同步；
- 多厂区统一管理；
- 自动企业微信/钉钉通知；
- 自动排班；
- 工资或绩效结算。

---

# 4. 用户角色

系统分为两类角色。

## 4.1 普通成员

主要使用手机访问。

权限：

- 查看本人信息；
- 开始实训；
- 结束实训；
- 上传现场照片；
- 查看本人今日/本周/本月统计；
- 查看本人历史实训记录；
- 查看本人月度达标情况。

普通成员不能：

- 修改他人数据；
- 修改自己的历史训练时长；
- 删除实训记录；
- 修改月度目标；
- 导出全员数据；
- 进入系统管理后台。

---

## 4.2 管理员

主要通过电脑端浏览器使用。

权限：

- 查看所有人员；
- 查看所有训练记录；
- 查看照片；
- 添加/修改/停用人员；
- 设置月度目标训练时长；
- 修改异常记录；
- 补录训练记录；
- 作废错误记录；
- 查看修改日志；
- 导出 Excel / CSV；
- 备份数据库；
- 查看数据统计和图表。

---

# 5. 核心业务流程

## 5.1 开始实训流程

流程：

```text
用户扫码/打开系统
        ↓
识别或选择本人
        ↓
进入个人首页
        ↓
点击【开始实训】
        ↓
系统显示姓名、所属车间、当前时间
        ↓
拍摄/上传现场照片
        ↓
点击【确认开始实训】
        ↓
系统保存开始记录
        ↓
状态变为【实训中】
```

开始打卡需要记录：

- 用户 ID；
- 姓名；
- 所属车间；
- 开始时间；
- 开始现场照片；
- 创建时间；
- 打卡设备信息（可选）；
- IP 地址（可选）。

---

## 5.2 结束实训流程

```text
用户进入个人首页
        ↓
系统识别当前存在未结束实训
        ↓
页面显示当前实训开始时间
        ↓
点击【结束实训】
        ↓
拍摄/上传结束现场照片
        ↓
点击【确认结束实训】
        ↓
系统保存结束时间
        ↓
自动计算本次实训时长
        ↓
自动更新日/周/月统计
```

结束打卡记录：

- 用户 ID；
- 对应开始记录 ID；
- 结束时间；
- 结束现场照片；
- 本次实训时长；
- 记录状态。

---

# 6. 打卡业务规则

## 6.1 基本规则

1. 一个用户同一时间只能存在一条“实训中”记录。
2. 已经处于实训中的用户不能重复开始实训。
3. 没有开始实训记录时，不能直接结束实训。
4. 单次实训时长 = 结束时间 - 开始时间。
5. 时长统一以分钟存储。
6. 页面展示时可转换为“X 小时 X 分钟”。
7. 所有时间计算以服务器时间为准。
8. 用户不能手动输入开始时间和结束时间。
9. 普通成员不能修改历史实训时间。

---

## 6.2 跨天实训

若一次实训跨越 00:00：

例如：

```text
9月19日 23:00 开始
9月20日 01:00 结束
```

系统按自然日拆分：

```text
9月19日：1小时
9月20日：1小时
```

但原始记录仍保留为一条完整训练记录。

---

# 7. 异常打卡机制

异常处理是系统的重要功能。

## 7.1 忘记结束打卡

若用户只有开始记录但长时间未结束，则标记：

```text
状态：异常 / 未结束
```

管理员可：

- 补录结束时间；
- 作废该记录。

补录时必须填写：

- 补录结束时间；
- 修改原因；
- 修改管理员；
- 修改时间。

---

## 7.2 超长实训检测

系统支持配置：

```text
单次最长正常实训时长
默认：8小时
```

如果超过该值：

```text
状态：疑似异常
```

记录仍保留，但进入异常记录列表。

---

## 7.3 重复打卡

若用户已有未结束记录，再次点击开始：

提示：

> 当前已有进行中的实训记录，请先结束本次实训。

---

## 7.4 管理员修改

任何管理员修改均必须保留原始数据。

审计内容包括：

- 原始开始时间；
- 原始结束时间；
- 修改后开始时间；
- 修改后结束时间；
- 修改前时长；
- 修改后时长；
- 修改人；
- 修改时间；
- 修改原因。

---

# 8. 月度训练达标规则

## 8.1 月目标

管理员可设置：

```text
月度目标实训时间
```

默认单位：

```text
小时
```

例如：

```text
16 小时/月
```

支持：

- 全员统一目标；
- 后期扩展为个人目标。

---

## 8.2 达标判断

```text
本月累计实训时间 >= 月度目标
```

则：

```text
达标
```

否则：

```text
未达标
```

完成率：

```text
月度完成率 = 本月实训时间 / 月度目标时间 × 100%
```

超过 100% 时可以显示真实比例，例如：

```text
137%
```

---

# 9. 统计口径

系统统一采用自然时间周期。

## 9.1 今日实训时间

统计范围：

```text
当日 00:00:00 - 23:59:59
```

显示：

```text
2小时15分钟
```

---

## 9.2 本周实训时间

默认：

```text
周一 00:00 - 周日 23:59:59
```

---

## 9.3 本月实训时间

统计当月全部有效实训时长。

不包括：

- 已作废记录；
- 无有效结束时间的异常记录。

---

## 9.4 实训频率

至少展示：

- 今日实训次数；
- 本周实训次数；
- 本月实训次数；
- 本月有训练的天数；
- 平均每次实训时长。

计算：

```text
平均每次实训时长 =
本月累计有效训练时间 ÷ 本月有效实训次数
```

---

# 10. 用户端页面设计

---

## 10.1 用户识别页

### 页面目标

让用户快速进入自己的个人训练页面。

### 页面字段

- 姓名；
- 所属车间；
- 进入系统按钮。

V1.0 推荐使用：

```text
姓名下拉选择 + 所属车间自动带出
```

后期可以使用本地缓存记住用户。

---

# 10.2 个人首页

页面主要内容：

```text
姓名：张三
所属车间：炼钢维修车间

今日实训
1小时35分钟

本周实训
4小时20分钟

本月实训
13小时40分钟 / 16小时

月度完成率
85%

状态
未达标

本月实训次数
8次
```

主按钮根据状态切换。

未在实训：

```text
【开始实训】
```

实训中：

```text
当前实训中
开始时间：14:32

已训练：01:26:35

【结束实训】
```

其他入口：

```text
【我的实训记录】
```

---

# 10.3 开始实训页面

字段：

- 姓名；
- 所属车间；
- 当前时间；
- 现场照片；
- 确认按钮。

按钮：

```text
【拍照/上传照片】
【确认开始实训】
```

要求：

- 照片必填；
- 显示图片预览；
- 提交前允许重新拍摄。

---

# 10.4 结束实训页面

显示：

- 姓名；
- 所属车间；
- 开始时间；
- 当前结束时间；
- 当前已实训时长；
- 结束现场照片。

按钮：

```text
【拍照/上传照片】
【确认结束实训】
```

---

# 10.5 我的实训记录

支持：

- 本月；
- 上月；
- 自定义日期。

表格：

| 日期 | 开始时间 | 结束时间 | 本次时长 | 状态 |
|---|---|---|---|---|
| 09-19 | 14:32 | 16:18 | 1h46m | 正常 |
| 09-17 | 09:10 | 10:25 | 1h15m | 正常 |

点击记录后进入详情。

---

# 10.6 实训详情

显示：

- 日期；
- 开始时间；
- 结束时间；
- 本次时长；
- 开始照片；
- 结束照片；
- 记录状态。

---

# 11. 管理后台

---

## 11.1 首页数据总览

顶部指标：

- 实训成员总数；
- 今日参与实训人数；
- 今日累计实训时间；
- 本周累计实训时间；
- 本月累计实训时间；
- 本月达标人数；
- 本月未达标人数；
- 当前正在实训人数；
- 异常记录数量。

---

# 11.2 人员实训总览表

字段：

| 字段 | 说明 |
|---|---|
| 姓名 | 用户姓名 |
| 所属车间 | 所属部门 |
| 今日实训 | 今日累计 |
| 本周实训 | 本周累计 |
| 本月实训 | 本月累计 |
| 月度目标 | 目标时长 |
| 完成率 | 月累计/目标 |
| 是否达标 | 达标/未达标 |
| 本月次数 | 有效训练次数 |
| 最近实训 | 最近一次训练时间 |
| 当前状态 | 未训练/实训中 |

支持：

- 搜索；
- 姓名筛选；
- 车间筛选；
- 达标/未达标筛选；
- 排序。

排序支持：

- 本月时长；
- 完成率；
- 本月实训次数；
- 最近实训时间。

---

# 11.3 人员详情页

管理员点击某个人后查看：

### 基础信息

- 姓名；
- 所属车间；
- 状态；
- 月度目标。

### 数据指标

- 今日实训；
- 本周实训；
- 本月实训；
- 月度完成率；
- 本月实训次数；
- 平均单次实训时长；
- 本月有训练天数。

### 图表

至少支持：

#### 图表 1：本月每日实训时长

类型：

```text
柱状图
```

X 轴：

```text
日期
```

Y 轴：

```text
小时
```

#### 图表 2：月度累计进度

类型：

```text
进度条 / 仪表式显示
```

示例：

```text
13.6 / 16 h
85%
```

#### 图表 3：周训练趋势

类型：

```text
折线图
```

---

# 11.4 实训记录管理

表格字段：

| 字段 | 说明 |
|---|---|
| 记录编号 | 唯一 ID |
| 姓名 | 成员 |
| 所属车间 | 部门 |
| 日期 | 实训日期 |
| 开始时间 | 开始打卡 |
| 结束时间 | 结束打卡 |
| 时长 | 本次有效时长 |
| 开始照片 | 查看 |
| 结束照片 | 查看 |
| 状态 | 正常/异常/作废 |
| 操作 | 查看/修改 |

支持筛选：

- 人员；
- 日期；
- 状态；
- 车间。

---

# 11.5 异常记录页面

显示：

- 未结束记录；
- 超长训练记录；
- 其他人工标记异常记录。

管理员操作：

```text
【补录结束时间】
【修改】
【作废记录】
```

所有操作必须填写：

```text
修改原因
```

---

# 11.6 人员管理

管理员可：

- 新增人员；
- 修改人员；
- 停用人员。

人员字段：

- 姓名；
- 所属车间；
- 手机号（可选）；
- 状态；
- 创建时间。

人员不能直接物理删除。

若人员离开：

```text
状态 = 停用
```

历史记录继续保留。

---

# 12. 数据可视化

管理员首页至少包含以下图表。

## 12.1 本月个人实训时长排名

类型：

```text
横向柱状图
```

展示全员本月累计实训时间。

---

## 12.2 月度达标情况

展示：

```text
达标人数
未达标人数
```

可使用：

- 环形图；
- 柱状图。

---

## 12.3 每日实训趋势

类型：

```text
折线图
```

指标：

```text
每天总实训时间
```

---

## 12.4 每人训练频率

展示：

```text
本月训练次数
```

---

# 13. 数据导出

管理员支持手动导出。

---

## 13.1 Excel 导出

文件名称：

```text
2026年09月实训记录.xlsx
```

建议包含 3 个 Sheet。

### Sheet1：人员月度汇总

字段：

| 姓名 | 所属车间 | 今日时长 | 本周时长 | 本月时长 | 月目标 | 完成率 | 是否达标 | 本月次数 |
|---|---|---|---|---|---|---|---|---|

---

### Sheet2：详细实训记录

字段：

| 姓名 | 所属车间 | 日期 | 开始时间 | 结束时间 | 本次时长 | 状态 | 开始照片路径 | 结束照片路径 |
|---|---|---|---|---|---|---|---|---|

---

### Sheet3：异常记录

字段：

| 姓名 | 日期 | 开始时间 | 异常类型 | 处理状态 | 修改人 | 修改原因 |
|---|---|---|---|---|---|---|

---

## 13.2 CSV 导出

支持导出：

- 人员统计 CSV；
- 实训记录 CSV。

---

## 13.3 照片导出

后期可支持：

```text
2026-09实训照片.zip
```

目录：

```text
2026-09实训照片/

├── 张三/
│   ├── 2026-09-03_083211_开始.jpg
│   ├── 2026-09-03_103522_结束.jpg
│   └── ...
│
├── 李四/
└── 王五/
```

---

# 14. 数据库设计

数据库：

```text
SQLite
```

---

## 14.1 users 用户表

建议字段：

```text
id
name
workshop
phone
status
created_at
updated_at
```

说明：

```text
status:
1 = 正常
0 = 停用
```

---

## 14.2 training_records 实训记录表

字段：

```text
id
user_id
start_time
end_time
duration_minutes
start_photo
end_photo
status
abnormal_type
created_at
updated_at
```

status：

```text
training   实训中
completed  已完成
abnormal   异常
void       已作废
```

---

## 14.3 monthly_targets 月度目标表

字段：

```text
id
year
month
target_minutes
created_at
updated_at
```

V1.0：

```text
全员统一目标
```

后续可扩展：

```text
user_id
```

实现个人目标。

---

## 14.4 admin_users 管理员表

字段：

```text
id
username
password_hash
display_name
status
created_at
```

密码必须以 Hash 形式保存，禁止明文存储。

---

## 14.5 audit_logs 审计日志表

字段：

```text
id
admin_id
action
record_type
record_id
before_data
after_data
reason
created_at
```

---

# 15. 照片存储

照片不存入 SQLite 二进制字段。

采用：

```text
数据库保存照片路径
+
照片保存到本地文件夹
```

建议目录：

```text
data/

├── training.db
│
├── photos/
│   ├── 2026/
│   │   ├── 09/
│   │   │   ├── 张三/
│   │   │   ├── 李四/
│   │   │   └── 王五/
│
├── exports/
│
└── backups/
```

文件名建议：

```text
用户ID_日期时间_类型.jpg
```

例如：

```text
003_20260919_143211_start.jpg
003_20260919_161805_end.jpg
```

---

# 16. 图片处理规则

为避免手机照片过大造成磁盘快速增长，上传后自动压缩。

建议：

```text
最长边：1600 px
JPEG Quality：75~85
```

单张图片建议控制在：

```text
300 KB - 1.5 MB
```

原始照片默认不保留。

---

# 17. 前后端技术方案

建议技术栈：

## 前端

```text
Vue 3
Vite
Element Plus
ECharts
```

## 后端

```text
Python 3
FastAPI
SQLAlchemy
```

## 数据库

```text
SQLite
```

## Excel

```text
openpyxl
```

## 图片处理

```text
Pillow
```

## Web Server

开发：

```text
Uvicorn
```

正式运行可采用：

```text
Uvicorn / Waitress
```

---

# 18. 本地部署方案

服务器：

```text
实训室现有 Windows 电脑
```

网络：

```text
实训室局域网 / Wi-Fi
```

示例服务器地址：

```text
192.168.1.100
```

用户访问：

```text
http://192.168.1.100:8000
```

后期可以配置局域网域名：

```text
http://training.local
```

---

# 19. 二维码入口

管理员生成固定二维码：

```text
实训室训练系统
```

二维码内容：

```text
http://服务器IP:端口
```

打印后张贴在：

- 实训室入口；
- 实训台附近；
- 公告板。

成员连接实训室 Wi-Fi 后扫码访问。

---

# 20. PWA 支持

后期将网页升级为 PWA。

成员可以：

```text
添加到手机桌面
```

桌面显示：

```text
电气实训
```

点击后直接进入系统。

PWA 不是 V1.0 必须条件，但建议在 V1.1 加入。

---

# 21. 登录与身份识别

## V1.0 推荐方案

普通成员：

```text
姓名选择 + 浏览器本地记忆
```

首次：

```text
选择姓名
```

以后：

```text
自动进入本人页面
```

管理员：

```text
用户名 + 密码
```

---

# 22. 安全要求

由于系统包含人员信息和现场照片，需要满足以下要求：

1. 系统默认只允许局域网访问；
2. 不开放公网端口；
3. 管理员后台必须登录；
4. 管理员密码加密保存；
5. 普通用户无法访问管理员接口；
6. 数据库定期备份；
7. 禁止客户端直接修改训练时长；
8. 所有训练时间以服务器时间为准；
9. 照片文件不能通过目录遍历任意访问；
10. 管理员修改必须保留审计日志。

---

# 23. 备份机制

管理员后台增加：

```text
【立即备份】
```

备份内容：

```text
training.db
+
photos
```

数据库建议：

```text
每日自动备份一次
```

目录：

```text
backups/

├── 2026-09-19/
│   └── training.db
├── 2026-09-20/
│   └── training.db
```

建议至少保留最近：

```text
30 天
```

---

# 24. 系统设置

管理员可设置：

- 实训室名称；
- 月度目标时长；
- 单次最长正常实训时长；
- 照片是否必填；
- 开始打卡是否要求照片；
- 结束打卡是否要求照片；
- 系统标题。

---

# 25. 首页视觉状态

建议使用状态颜色：

```text
绿色：已达标
橙色：未达标
红色：异常
蓝色：实训中
灰色：未训练
```

注意：

颜色不能作为唯一状态表达方式。

必须同时显示文字：

```text
已达标
未达标
异常
实训中
```

---

# 26. 响应式设计要求

系统需要支持：

## 手机端

宽度：

```text
320px - 768px
```

主要用于：

- 开始实训；
- 结束实训；
- 查看个人记录。

操作按钮必须适合手指点击。

---

## PC 端

推荐：

```text
1366 × 768
1920 × 1080
```

主要用于：

- 管理后台；
- 表格；
- 数据统计；
- 图表；
- 导出。

---

# 27. 性能要求

由于系统规模只有约 15 人：

目标：

```text
页面首次加载 < 2 秒
普通接口响应 < 500 ms
统计页面 < 1 秒
```

数据库规模按：

```text
15人 × 每天2次 × 365天 × 5年
```

完全可以由 SQLite 处理。

---

# 28. 数据生命周期

系统原则：

```text
历史实训数据永久保留
```

除非管理员明确执行清理操作。

人员停用后：

```text
历史记录不删除
```

作废记录：

```text
保留记录
status = void
```

不执行物理删除。

---

# 29. 主要 API 规划

后端建议按照 REST API 设计。

---

## 用户

```text
GET /api/users
GET /api/users/{id}
POST /api/users
PUT /api/users/{id}
```

---

## 开始实训

```text
POST /api/training/start
```

参数：

```text
user_id
photo
```

---

## 结束实训

```text
POST /api/training/end
```

参数：

```text
user_id
photo
```

---

## 当前实训状态

```text
GET /api/training/current/{user_id}
```

---

## 个人统计

```text
GET /api/statistics/user/{user_id}
```

---

## 全员统计

```text
GET /api/statistics/overview
```

---

## 实训记录

```text
GET /api/training/records
GET /api/training/records/{id}
```

---

## 导出

```text
GET /api/export/monthly
GET /api/export/records
```

---

# 30. 关键验收标准

---

## 30.1 开始实训

给定：

```text
用户未处于实训中
```

操作：

```text
上传照片
点击开始实训
```

结果：

- 成功创建记录；
- 开始时间正确；
- 照片保存成功；
- 状态变为实训中。

---

## 30.2 防止重复开始

给定：

```text
用户已有未结束记录
```

再次开始：

系统必须拒绝。

---

## 30.3 正常结束

给定：

```text
用户存在实训中记录
```

结束后：

- 记录结束时间；
- 保存结束照片；
- 自动计算时长；
- 更新统计数据；
- 状态变为已完成。

---

## 30.4 日统计

若用户当天有：

```text
09:00 - 10:00
14:00 - 16:00
```

今日实训必须显示：

```text
3小时
```

---

## 30.5 周统计

统计范围必须为：

```text
周一 - 周日
```

---

## 30.6 月度达标

假设：

```text
月目标 = 16小时
本月累计 = 16小时
```

必须显示：

```text
达标
100%
```

---

## 30.7 跨天训练

记录：

```text
23:00 - 01:00
```

日统计：

```text
第一天：1小时
第二天：1小时
```

---

## 30.8 异常记录

未结束记录：

必须进入：

```text
异常记录
```

管理员能够：

- 补录；
- 作废。

---

## 30.9 Excel 导出

导出必须保证：

- 中文正常；
- 时间格式正常；
- 时长正确；
- 汇总结果与系统一致；
- 文件可由 Microsoft Excel 正常打开。

---

# 31. 首版开发优先级

采用：

```text
P0
P1
P2
```

---

## P0：必须完成

- 人员管理；
- 开始实训；
- 结束实训；
- 上传照片；
- 实训状态；
- 单次时长计算；
- 今日统计；
- 本周统计；
- 本月统计；
- 月度达标；
- 管理员登录；
- 实训记录查询；
- Excel 导出；
- SQLite 数据库；
- 本地照片保存。

---

## P1：建议完成

- 管理首页；
- 数据图表；
- 异常记录；
- 管理员补录；
- 审计日志；
- 数据备份；
- CSV 导出；
- 实训频率统计。

---

## P2：后续优化

- PWA；
- 自动记住用户；
- 照片 ZIP 导出；
- 多月统计；
- 年度统计；
- 移动端数据图表；
- 自动备份；
- 系统配置页面。

---

# 32. 推荐开发阶段

## 第一阶段：基础框架

完成：

```text
Vue + FastAPI + SQLite
```

建立：

- 项目结构；
- 数据库；
- 用户管理；
- 管理员登录。

---

## 第二阶段：打卡核心

完成：

- 开始实训；
- 结束实训；
- 图片上传；
- 时长计算；
- 实训状态。

---

## 第三阶段：统计

完成：

- 日统计；
- 周统计；
- 月统计；
- 达标判断；
- 实训频率。

---

## 第四阶段：管理后台

完成：

- 人员管理；
- 实训记录；
- 异常处理；
- 数据看板。

---

## 第五阶段：导出和备份

完成：

- Excel；
- CSV；
- 数据库备份；
- 照片管理。

---

## 第六阶段：UI 与移动端优化

完成：

- 手机适配；
- 图表；
- PWA；
- 二维码入口。

---

# 33. 推荐项目目录

```text
training-system/

├── frontend/
│   ├── src/
│   ├── public/
│   └── package.json
│
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── routers/
│   │   ├── services/
│   │   └── utils/
│   │
│   └── requirements.txt
│
├── data/
│   ├── training.db
│   ├── photos/
│   ├── exports/
│   └── backups/
│
├── scripts/
│
├── README.md
│
└── PRD.md
```

---

# 34. 后续可扩展功能

系统稳定后可考虑：

1. 微信扫码快速登录；
2. 企业微信集成；
3. NFC 实训卡；
4. 人脸识别；
5. 局域网位置校验；
6. 每周训练排行榜；
7. 月度训练报告；
8. 年度训练统计；
9. 自动生成个人训练档案；
10. PDF 月度报告；
11. 多实训室管理；
12. 设备训练项目记录；
13. 每次训练选择实训项目；
14. 实训内容评价；
15. 管理员审核；
16. 培训积分系统。

---

# 35. V1.0 最终产品定义

V1.0 产品定位：

> 一套部署在实训室内部 Windows 电脑上的轻量级 Web 实训管理系统。

普通成员：

```text
手机连接实训室 Wi-Fi
        ↓
扫码进入系统
        ↓
选择本人
        ↓
拍照
        ↓
开始实训
        ↓
完成训练
        ↓
拍照
        ↓
结束实训
```

管理员：

```text
浏览器进入管理后台
        ↓
查看人员训练情况
        ↓
查看日/周/月数据
        ↓
查看月度达标情况
        ↓
查看异常记录
        ↓
导出 Excel
        ↓
本地保存
```

系统技术原则：

```text
本地部署
低维护
低成本
数据可控
无需长期云服务
手机使用方便
统计自动化
```

---

# 36. V1.0 成功标准

系统正式投入使用后，应达到：

- 15 名左右成员可以稳定打卡；
- 每次实训都有准确开始和结束时间；
- 每次实训均可关联现场照片；
- 日、周、月累计时长自动生成；
- 月度达标状态无需人工计算；
- 管理员可以随时查看全员训练情况；
- 月底可一键导出数据；
- 所有数据保存在本地；
- 系统断网情况下局域网内仍可正常使用；
- 不依赖收费云服务即可长期运行。
